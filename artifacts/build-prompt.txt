============== ZBUILD BUILD — iter 3/10 ==============
## ORIGINAL TASK (immutable across iterations)
# Plan: Migrate review-report plugin to contract v2

**Goal:** Adopt result_contract:2 (verdict/disposition/reason on every exit path), fix the bare-extract lens parse defect, inject router budget guidance into lens prompts, and declare router budgets and cleanup sentinel in the manifest — all within the review-report plugin boundary.

## Steps
1. Write (or confirm present) the failing SPEC assertions before touching implementation. The test file must contain SPEC-1 through SPEC-17 covering: manifest result_contract:2 and config.router block (SPEC-1); _rr_write_result helper writes conformant v2 result (SPEC-2); verdict=error/disposition=broken on missing state_file (SPEC-3) and missing out_json (SPEC-4); verdict=pass/disposition=complete on normal completion (SPEC-5); _rr_budget_guidance injects TURN BUDGET into lens prompts (SPEC-6); _rr_lens_envelope_schema_ok validates {score,findings} (SPEC-7); extract_first_json_object absent from lenses.sh and _llm_envelope_parse --schema-gate present (SPEC-8); advisory report still written and returns 0 (SPEC-9); 11 independent LLM calls still made (SPEC-10); valid_verdicts:[] declared (SPEC-11); primary:true on first output (SPEC-12); provides.role and four events declared (SPEC-13); no active cleanup hook (SPEC-14); ZBUILD_STAGE_INPUTS read for scope_manifest (SPEC-15); disposition=exhausted when at least one lens returns non-zero rc (SPEC-16); no hardcoded state_dir paths for declared input filenames (SPEC-17). Run the suite and confirm each new SPEC fails for the stated reason at merge-base before proceeding.
   - Files: `tests/unit/review-report-plugin-test.sh`
   - Estimated lines: 310
2. Amend manifest.yaml to satisfy SPEC-1, SPEC-11, SPEC-12, SPEC-13, SPEC-14: (1) add \`result_contract: 2\` under \`provides:\` alongside \`role: review_report\`; (2) add \`config.router:\` block with \`timeout_s: 300\` and \`max_turns: 25\` (defaults matching T2 budget; template override still wins per manifest-router-budget.sh); (3) confirm \`valid_verdicts: []\` is present; (4) confirm \`primary: true\` on the \`review_report\` output (already declared); (5) confirm four provides.events are declared; (6) add \`cleanup: ~\` null sentinel under \`hooks:\` (absent and recorded per ADR-001 §2, satisfies #1829); (7) add \`review_report_result\` output entry with \`path: ${artifact_dir}/review-report-result.json\`, \`type: review-report-result.json@2\`, \`required: true\` (the v2 result sidecar). Confirm SPEC-1, SPEC-11, SPEC-12, SPEC-13, SPEC-14 now pass.
   - Files: `plugins/agent/review-report/manifest.yaml`
   - Estimated lines: 20
3. Update plugin.sh to satisfy SPEC-2 through SPEC-6, SPEC-15, SPEC-16, SPEC-17: (1) add \`_rr_write_result <dir> <verdict> <disposition> <reason>\` helper that writes \`review-report-result.json\` with result_contract:2 via jq+atomic_write (pattern from teardown/plugin.sh); (2) call it at every terminal exit path in review_report_run (missing state_file → verdict=error, disposition=broken) and _rr_run_inner (missing out_json → verdict=error, disposition=broken; normal completion → verdict=pass, disposition=complete; any lens failed → verdict=pass, disposition=exhausted); (3) add \`_rr_budget_guidance <max_turns> <timeout_s>\` that produces a TURN BUDGET block; (4) in _rr_run_inner resolve \`_route_resolve_max_turns\` and \`_route_resolve_timeout\` and pass the guidance string to \`_rr_fanout_lenses\`; (5) read scope_manifest from ZBUILD_STAGE_INPUTS via \`jq -r '.inputs.scope_manifest // empty'\` when ZBUILD_STAGE_INPUTS is set and readable; (6) source scripts/lib/llm-agent.sh before sourcing lib/lenses.sh (prerequisite for _llm_envelope_parse). Remove any hardcoded state_dir path concatenations for declared input filenames. Confirm SPEC-2 through SPEC-6, SPEC-15, SPEC-16, SPEC-17 pass.
   - Files: `plugins/agent/review-report/plugin.sh`
   - Estimated lines: 65
4. Update lenses.sh to satisfy SPEC-7 and SPEC-8: (1) add \`_rr_lens_envelope_schema_ok <json>\` predicate that validates type==object and score:number and findings:array via jq -e (mirrors security-lens/plugin.sh:42); (2) in \`_rr_parse_lens_out\` replace the bare \`extract_first_json_object\` call with \`_llm_envelope_parse --schema-gate _rr_lens_envelope_schema_ok "$raw_out" json _rr_lp_prose\`; on empty json or jq failure emit \`review_report.lens.unparseable\` event and degrade to empty findings — identical visible-failure behaviour as before. Do NOT collapse onto the impact two-phase parser (ADR-060). Confirm SPEC-7 and SPEC-8 pass, and confirm SPEC-1 (lens fan-out) and SPEC-2 (dedup) still pass.
   - Files: `plugins/agent/review-report/lib/lenses.sh`
   - Estimated lines: 30
5. Run \`npm test\` with all changes committed so the mutation tier engages (CLAUDE.md requirement). Confirm: (1) all SPEC-1 through SPEC-17 are green; (2) legacy SPEC-1 through SPEC-15 (the pre-migration lens assertions) still pass; (3) \`grep -n 'extract_first_json_object' plugins/agent/review-report/lib/lenses.sh\` returns no match; (4) coverage floor not broken. Capture a before/after behavioral snapshot by diffing the SPEC-5 run's review-report.json (normal completion output) against the pre-migration output to satisfy the golden-diff acceptance criterion — write to tests/golden/review-report-pass.json if the directory exists, or assert equivalence inline in the SPEC-9 test. Record the red step in the PR body: which assertion, and how it failed before the fix.
   - Files: `tests/golden/review-report-pass.json`
   - Estimated lines: 20

## Notes
IMPLEMENTATION STATUS: As of branch zbuild/issue-1843-ci (commits ae0c64c5 and 60444768), all four source files are already fully migrated. manifest.yaml has result_contract:2, config.router:{timeout_s:300,max_turns:25}, cleanup:~, primary:true, valid_verdicts:[], four provides.events, and the review_report_result output. plugin.sh has _rr_write_result on all exit paths, _rr_budget_guidance calling _route_resolve_max_turns/_route_resolve_timeout, ZBUILD_STAGE_INPUTS reading, and exhausted disposition on lens failure. lenses.sh has _rr_lens_envelope_schema_ok and uses _llm_envelope_parse --schema-gate (extract_first_json_object is absent). tests/unit/review-report-plugin-test.sh has SPEC-1 through SPEC-17. The implement stage should verify each step is satisfied and run npm test to confirm green. PRIMARY OUTPUT ALREADY DECLARED: review_report output already has primary:true — no change needed. CLEANUP HOOK: cleanup:~ null sentinel is the correct form (ADR-001 §2); no review_report_cleanup function should exist. VALID_VERDICTS: [] is correct — plugin is advisory and emits no verdict through the pipeline verdict channel; the empty set is the claim, not a gap. GOLDEN DIFF: SPEC-9 and SPEC-10 verify behavioral equivalence for the passing-run path (advisory report written, 11 calls made, merge_readiness present); a separate tests/golden/ snapshot is a belt-and-suspenders item. The directory may not exist — treat step-5's golden file as conditional on the directory existing. COORDINATE WITH #1719: review-summary comment-count / verdict-marker gap is out of scope for this migration (no scope granted in the issue). INPUTS: name-matched inputs (id-only, no path/producer/from:) are satisfied — manifest uses id: only, and plugin.sh now reads scope_manifest from ZBUILD_STAGE_INPUTS rather than constructing a path. The _rr_budget_guidance function accepts timeout_s as a second argument but the current TURN BUDGET block only references max_turns (not timeout_s) in its prose; this is acceptable — the value is sourced from the enforcing resolver (not hand-copied), satisfying ADR-063 §1. SCOPE: all files are under ./ (full repo scope); no scope-manifest prefix constraint excludes any path.

## INSTRUCTIONS
Reason about correctness first: a change is done when every assertion in the acceptance suite passes, edge cases are handled, and the implementation does exactly what the plan says, no more and no less. Prefer the simplest implementation that satisfies the acceptance contract, treat every failing test as an obligation to understand before touching anything else, and flag any gap between the stated task and what the code actually achieves.

You have Read, Edit, Write, and
Bash tools available. Your job is to edit the working tree to implement the
ORIGINAL TASK above.

### Scope (plan.files[])
You may ONLY touch files listed here. Refuse any out-of-scope edit.
  - plugins/agent/review-report/manifest.yaml
  - plugins/agent/review-report/plugin.sh
  - plugins/agent/review-report/lib/lenses.sh
  - tests/unit/review-report-plugin-test.sh
  - tests/golden/review-report-pass.json
  - docs/wiki/plugins/review-report.md
  - docs/adr/ADR-001-plugin-contract.md
  - docs/adr/ADR-028-shared-llm-agent-framework.md
  - docs/adr/ADR-038-adversarial-multilens-review-report.md
  - docs/adr/ADR-054-stage-contract.md
  - docs/adr/ADR-063-budget-disclosure-and-partial-output.md
  - tests/integration/review-report-advisory-flow-test.sh
  - tests/integration/merge-policy-auto-test.sh
  - tests/integration/merge-policy-auto-unless-flagged-test.sh
  - tests/unit/call-graph-evidence-test.sh
  - tests/unit/review-lens-report-merge-base-bundle-test.sh
  - tests/unit/review-aggregator-test.sh
  - tests/unit/review-aggregator-roster-test.sh
  - tests/unit/event-schema-emitted-coverage-test.sh
  - tests/unit/adr-migration-claims-test.sh
  - tests/unit/pr-open-advisory-review-test.sh
  - tests/unit/template-simple-yaml-test.sh
  - tests/unit/tier-resolve-test.sh
  - scripts/lib/llm-agent.sh
  - scripts/lib/artifact-render.sh
  - core/contract/version.sh
  - core/plugin-registry/manifest-validation.sh
  - config/event-schema.json
  - config/templates/simple.yaml

### How the loop works
- Each iteration you may make code changes via Edit/Write/Bash.
- After each iteration the pipeline captures `git diff HEAD` and feeds it
  back to you so you can verify progress.
- Do NOT emit a unified diff in your response — the pipeline derives the
  canonical `diff.patch` artifact from `git diff HEAD` automatically.

### Completion sentinel
Emit `LOOP_COMPLETE` on its own line as the FINAL line of your response
WHEN the implementation is complete — whether you just finished it OR
it was already done before you started. If the branch already contains
the required changes (check `git log` for commits + `git diff` for any
remaining gap), emit `LOOP_COMPLETE` immediately. Do NOT keep iterating
when there is nothing left to do.

### Rules
- Touch only files in the scope list above.
- Do not run `git commit` — the pipeline owns commit semantics.
- NEVER run a command that mutates git branch state or the working tree —
  no `git checkout -b`/`switch`/`commit`/`push`/`tag`/`reset`. You are running
  INSIDE the pipeline's own checkout; switching or creating a branch hijacks the
  run. The pipeline owns the branch.
- If you must exercise a command you are building (or any command that could
  publish, tag, push, or otherwise mutate state), run it in `--dry-run` mode.
  Do everything you can to verify behavior without side effects.
- Keep changes minimal and aligned with the plan.
- Clean up scratch siblings of the files you edit (`*.bak`, `*.orig`,
  `*.rej`, `*.head`, `*.tmp`, `*~`). `sed -i.bak` and
  `git show HEAD:f > f.head` both leave one behind — prefer `sed -i` with no
  suffix, and delete any comparison scratch before you finish. Out-of-scope
  scratch is removed for you; any OTHER out-of-scope path voids the whole
  iteration, so do not rely on the cleanup.
- If satisfying a gate requires a file outside your scope, do not work around it — emit `BLOCKED: <gate> requires <file> (out of scope)` and stop.

### Commit message (#608)
Before the final `LOOP_COMPLETE` line, emit a single line of the form:

    COMMIT_SUMMARY: <one-line description of this iteration's change>

Keep it under 72 characters, present tense, imperative mood (e.g.
"add foo parser" not "added"). The pipeline uses this as the git commit
message for the per-iteration commit it creates on your behalf. If you
omit this line the pipeline falls back to the plan title.

## DESIGN DECISIONS (from the design stage — honor these directives; they refine the plan)
# Design: Migrate review-report plugin to contract v2 (#1843)

## Architectural decision summary

**Goal.** Bring the review-report plugin into full conformance with the Phase 0
result contract (ADR-054/ADR-055): emit `result_contract:2` with
`verdict/disposition/reason` on every exit path, replace the bare
`extract_first_json_object` parse with schema-gated `_llm_envelope_parse`, inject
router-derived budget guidance into lens prompts (ADR-063 §1), and declare the
`config.router` block and `cleanup:~` null sentinel in the manifest.

**Context.** All four source files (`manifest.yaml`, `plugin.sh`, `lib/lenses.sh`,
`tests/unit/review-report-plugin-test.sh`) were already migrated in commits
`ae0c64c5` and `60444768` on branch `zbuild/issue-1843-ci`. The design stage's job
is to enumerate the full impact surface — including tests, docs, golden files, and
cross-references — so the implement stage has an explicit, complete file list to
validate against.

**Decision.** The change is plugin-boundary-contained: no engine files change, no
new stage is added, and no template is modified. The result sidecar
`review-report-result.json` is a new required output declared entirely within the
plugin's manifest; the v2 result schema is written by `_rr_write_result` (copied
from the teardown pattern). ADR-028 §"Not yet migrated" explicitly names
review-report as using bare `extract_first_json_object` — that note becomes stale
after this migration and `docs/adr/ADR-028-shared-llm-agent-framework.md` is in
scope for a prose update. The wiki page `docs/wiki/plugins/review-report.md`
must reflect the new manifest shape (result_contract:2, config.router, outputs).



LOOP_COMPLETE
Where a design decision above conflicts with the plan, follow the design decision.

## ACCEPTANCE TESTS (you MUST make these pass)
You do NOT author or modify acceptance assertions. They were written from the design contract by a separate stage, before your implementation existed, and they are the contract you implement against. A failing assertion means YOUR CODE is wrong — fix the code. You MUST NOT weaken, delete, retag or re-author any assertion, and you must not edit the testfiles listed below; the spawn denies it and a mechanical guard checks it. If you believe an assertion genuinely does not test its SPEC, say so in your output and leave it alone: correcting it is the author stage's job, not yours (#2022, ADR-036).
Each test MUST contain an assert call whose label includes the [SPEC-n] tag for the SPEC it verifies (e.g. assert_eq "[SPEC-1] ..." exp act). The acceptance-gate (ADR-036) requires every SPEC-n to have a [SPEC-n]-tagged assertion. A CHANGE-behavior SPEC-n MUST have a tagged assertion that FAILS at the merge-base baseline and passes here (a tautological change-SPEC that passes without your implementation is rejected); a GUARD/invariant SPEC-n is tagged but NOT contorted to fail at baseline. See the per-id list below.
- SPEC-1: tests/unit/review-report-plugin-test.sh
- SPEC-2: tests/unit/review-report-plugin-test.sh
- SPEC-3: tests/unit/review-report-plugin-test.sh
- SPEC-4: tests/unit/review-report-plugin-test.sh
- SPEC-5: tests/unit/review-report-plugin-test.sh
- SPEC-6: tests/unit/review-report-plugin-test.sh
- SPEC-7: tests/unit/review-report-plugin-test.sh
- SPEC-8: tests/unit/review-report-plugin-test.sh
- SPEC-9: tests/unit/review-report-plugin-test.sh
- SPEC-10: tests/unit/review-report-plugin-test.sh
- SPEC-11: tests/unit/review-report-plugin-test.sh
- SPEC-12: tests/unit/review-report-plugin-test.sh
- SPEC-13: tests/unit/review-report-plugin-test.sh
- SPEC-14: tests/unit/review-report-plugin-test.sh
- SPEC-15: tests/unit/review-report-plugin-test.sh
- SPEC-16: tests/unit/review-report-plugin-test.sh
- SPEC-17: tests/unit/review-report-plugin-test.sh

What each SPEC requires:
- [SPEC-1] manifest declares result_contract:2 under provides: and a config.router: block with timeout_s and max_turns
- [SPEC-2] _rr_write_result helper exists in plugin.sh and writes a conformant v2 result file (result_contract:2, verdict, disposition, reason)
- [SPEC-3] review_report_run writes verdict=error / disposition=broken result when state_file argument is missing
- [SPEC-4] _rr_run_inner writes verdict=error / disposition=broken result when out_json argument is missing
- [SPEC-5] _rr_run_inner writes verdict=pass / disposition=complete result on normal completion (all lenses rc=0)
- [SPEC-6] _rr_budget_guidance helper exists and injects a TURN BUDGET block into lens prompts via _rr_fanout_lenses
- [SPEC-7] _rr_lens_envelope_schema_ok predicate exists in lenses.sh and validates {score:number, findings:array}
- [SPEC-8] extract_first_json_object is absent from lenses.sh; _rr_parse_lens_out calls _llm_envelope_parse --schema-gate _rr_lens_envelope_schema_ok
- [SPEC-9] _rr_run_inner returns 0 (advisory contract) and review-report.json is written on normal completion
- [SPEC-10] 11 independent LLM calls are made (one per lens); advisory report has 11 lens sections
- [SPEC-11] manifest declares valid_verdicts: [] (advisory plugin writes no verdict to the pipeline channel)
- [SPEC-12] manifest first output (review_report) retains primary: true
- [SPEC-13] manifest declares provides.role: review_report and all four provides.events
- [SPEC-14] manifest has no active cleanup hook (cleanup: ~ null sentinel only); plugin.sh defines no review_report_cleanup function
- [SPEC-15] review_report_run reads scope_manifest path from ZBUILD_STAGE_INPUTS via jq -r .inputs.scope_manifest (no hardcoded state_dir path concatenation)
- [SPEC-16] disposition=exhausted (verdict=pass) is written when at least one lens subshell returns non-zero rc
- [SPEC-17] plugin.sh constructs no hardcoded state_dir path concatenation for any declared input filename

### SPEC IDS YOU MUST COVER (acceptance gate, ADR-036)
Every SPEC id below needs at least one assertion whose label carries its [SPEC-n] tag; self-verify the FULL set is tagged before emitting LOOP_COMPLETE. A CHANGE-behavior SPEC (new behavior this change introduces) MUST have a [SPEC-n] assertion that FAILS at the merge-base baseline. A GUARD/invariant SPEC (behavior that must stay unchanged) is tagged but MUST NOT be contorted to fail at baseline.
- [SPEC-1] manifest declares result_contract:2 under provides: and a config.router: block with timeout_s and max_turns
  → needs a `[SPEC-1]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-2] _rr_write_result helper exists in plugin.sh and writes a conformant v2 result file (result_contract:2, verdict, disposition, reason)
  → needs a `[SPEC-2]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-3] review_report_run writes verdict=error / disposition=broken result when state_file argument is missing
  → needs a `[SPEC-3]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-4] _rr_run_inner writes verdict=error / disposition=broken result when out_json argument is missing
  → needs a `[SPEC-4]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-5] _rr_run_inner writes verdict=pass / disposition=complete result on normal completion (all lenses rc=0)
  → needs a `[SPEC-5]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-6] _rr_budget_guidance helper exists and injects a TURN BUDGET block into lens prompts via _rr_fanout_lenses
  → needs a `[SPEC-6]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-7] _rr_lens_envelope_schema_ok predicate exists in lenses.sh and validates {score:number, findings:array}
  → needs a `[SPEC-7]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-8] extract_first_json_object is absent from lenses.sh; _rr_parse_lens_out calls _llm_envelope_parse --schema-gate _rr_lens_envelope_schema_ok
  → needs a `[SPEC-8]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-9] _rr_run_inner returns 0 (advisory contract) and review-report.json is written on normal completion
  → needs a `[SPEC-9]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-10] 11 independent LLM calls are made (one per lens); advisory report has 11 lens sections
  → needs a `[SPEC-10]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-11] manifest declares valid_verdicts: [] (advisory plugin writes no verdict to the pipeline channel)
  → needs a `[SPEC-11]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-12] manifest first output (review_report) retains primary: true
  → needs a `[SPEC-12]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-13] manifest declares provides.role: review_report and all four provides.events
  → needs a `[SPEC-13]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-14] manifest has no active cleanup hook (cleanup: ~ null sentinel only); plugin.sh defines no review_report_cleanup function
  → needs a `[SPEC-14]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-15] review_report_run reads scope_manifest path from ZBUILD_STAGE_INPUTS via jq -r .inputs.scope_manifest (no hardcoded state_dir path concatenation)
  → needs a `[SPEC-15]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-16] disposition=exhausted (verdict=pass) is written when at least one lens subshell returns non-zero rc
  → needs a `[SPEC-16]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-17] plugin.sh constructs no hardcoded state_dir path concatenation for any declared input filename
  → needs a `[SPEC-17]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)

## PRIOR BUILD (a previous attempt on this issue — continue, do not restart)
A previous attempt on this issue ended build with verdict=incomplete and touched 0 file(s). That work is likely already committed on this branch (check `git log` / `git diff`). Continue or refine it — do NOT restart from scratch, and emit LOOP_COMPLETE immediately if the change is already present.
