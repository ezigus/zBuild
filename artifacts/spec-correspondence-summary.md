## spec-correspondence — partial

- judged 19 SPEC(s): 10 correspond, 9 partial, 0 mismatch, 0 uncheckable, 0 unjudged

- SPEC-7 partial: The first and third clauses are fully checked (yaml_get on the exact path for result_contract, and validate_manifest exit-code), but the second clause is tested by `grep -A5 'valid_verdicts'` on the raw file, which matches the string `valid_verdicts` at any nesting level — so it does not establish that the declarations are specifically under the `config.` path as the requirement states.
- SPEC-9 partial: The assertion checks `findings | length` (entry count == 1) but does not inspect the content of the finding entry, so "unmodified" for findings[] is unestablished — the array could contain a different or mutated element and still pass.
- SPEC-10 partial: The assertion verifies coercion token absence and the non-false-positive property, but checks only for the plain string "verdict" anywhere in the function body — it does not establish that verdict appears specifically as a jq field rather than in a comment, variable name, or echo statement, leaving the "only as a jq field" clause unestablished.
- SPEC-11 partial: The assertion checks only for literals matching `lens-*` patterns and `scope-manifest.md`, leaving other possible artifact path literals (e.g., `review-report.md`, `gate-result.json`, `findings.json`) uncovered, so a passing run does not establish that the body contains *no* hardcoded artifact path literals.
- SPEC-12 partial: The grep checks for `primary: true` anywhere in `manifest.yaml`, but the requirement specifies it must be declared on the `outputs[].lens_result` entry specifically — the assertion would pass if `primary: true` appeared on any other output or field, without establishing that `lens_result` bears it.
- SPEC-14 partial: The assertion checks that "99" appears somewhere in the prompt AND that "TURN BUDGET" appears somewhere in the prompt as two independent `grep -q` passes, but does not establish that the sentinel 99 is specifically located within the budget block — the two patterns could each match from unrelated prompt content while the budget block still reflects the manifest value.
- SPEC-16 partial: The assertion confirms all three named events are present and that exactly three `review_lens.` entries exist, but the awk extracts the entire `provides:` block rather than the `provides.events` sub-key specifically — a passing run does not establish that the events are nested under `events:` rather than some other sub-key within `provides:`.
- SPEC-18 partial: The assertion confirms the three named forbidden fields are absent and that `id` and `required` are present, but does not establish "only" — any other undeclared field (e.g., `description`, `default`, `optional`) would pass unchallenged, leaving the exclusivity half of the requirement unverified.
- SPEC-19 partial: The second grep matches `ADR-054.*§7` anywhere in the file but does not constrain the match to a YAML comment line (`#`-prefixed), so it cannot establish that the manifest carries an "explanatory comment" as the requirement states — the reference could appear in a value, key, or description string and still pass.

