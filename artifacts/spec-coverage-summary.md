## spec-coverage — covered

- All eight acceptance checkboxes in the issue map to declared SPECs — v2 results on every exit path (SPEC-1–4), valid_verdicts declared and tested (SPEC-7), no hardcoded paths (SPEC-14), router budgets with override precedence (SPEC-11/12), passing-run behavior (SPEC-9), primary output declared (SPEC-13), and cleanup hook (SPEC-5/6); `provides.events` and `provides.role` appear only in the contextual "What this plugin adopts" list, not in the enumerated acceptance checkboxes.

- every requirement the issue states maps to a declared SPEC
