# Intake Stage Prompt

**Phase 0.5 stub** — no LLM call this phase. Prompt reserved for the real
planning-driven intake (follow-on issue).

When the LLM call is wired, this prompt will:
1. Receive the sanitized goal + detected platforms.
2. Produce a structured scope recommendation (folders, roles, strategy).
3. Return a refined scope-manifest that downstream stages consume.
