## spec-coverage — covered

- Every behavioral requirement from the issue's acceptance checkboxes and both "also land here" items (schema-gate parser via SPEC-7/8, budget disclosure via SPEC-6, disposition=exhausted via SPEC-16) maps to at least one SPEC.

- every requirement the issue states maps to a declared SPEC
