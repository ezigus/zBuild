{"schema_version":1,"verdict":"incomplete","missing":[{"step_id":"SPEC-9","files_to_add":["core/pipeline/verdict.sh"],"reason":"`pr-open/manifest.yaml` will declare `valid_verdicts: [pass, blocked, error]`; `scripts/lib/lint-verdict-classify.sh` (already in scope) sweeps every primary-output manifest and calls `verdict_classify` on each declared verdict — `blocked` is not in `verdict_classify`'s case table (the table has `block` not `blocked`), so the lint flags it as unknown and exits 1; `tests/unit/lint-verdict-classify-test.sh` SPEC-6 runs the lint against the real plugins tree and will fail without this fix.","evidence":"`core/pipeline/verdict.sh` line 163: `fail|error|block|scope_violation|corrupt_diff` — `block` is present but `blocked` is absent; `scripts/lib/lint-verdict-classify.sh` line 142: `cls=\"$(verdict_classify \"$v\")\"` with line 143: `if [[ \"$cls\" == \"unknown\" ]]` triggers the violation path; `tests/unit/lint-verdict-classify-test.sh` line 121: `_run_lint \"$REPO_ROOT/plugins\"` sweeps all real plugin manifests."}]}
