## spec-coverage — covered

- Every checkbox requirement in the issue maps to a SPEC — v2 result on all exit paths (SPEC-1–4, 18), valid_verdicts (SPEC-7), no hardcoded artifact paths (SPEC-14), router budgets (SPEC-11–12), golden diff (SPEC-15), primary output (SPEC-13), provides.events and provides.role (SPEC-16–17), cleanup hook (SPEC-5), and name-matched inputs (SPEC-20).

- every requirement the issue states maps to a declared SPEC
