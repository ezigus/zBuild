# Design-gate: structural violations

The design contract is not build-ready. Fix these and re-emit design.md:

- GUARD_REGRESSED_AT_BASELINE SPEC-1 (tagged [guard] but its assertion FAILS at the merge-base — a guard holds there by definition; if this SPEC describes a change, tag it [change])
- GUARD_REGRESSED_AT_BASELINE SPEC-2 (tagged [guard] but its assertion FAILS at the merge-base — a guard holds there by definition; if this SPEC describes a change, tag it [change])
- GUARD_REGRESSED_AT_BASELINE SPEC-3 (tagged [guard] but its assertion FAILS at the merge-base — a guard holds there by definition; if this SPEC describes a change, tag it [change])
- GUARD_REGRESSED_AT_BASELINE SPEC-4 (tagged [guard] but its assertion FAILS at the merge-base — a guard holds there by definition; if this SPEC describes a change, tag it [change])
- GUARD_REGRESSED_AT_BASELINE SPEC-7 (tagged [guard] but its assertion FAILS at the merge-base — a guard holds there by definition; if this SPEC describes a change, tag it [change])
- GUARD_REGRESSED_AT_BASELINE SPEC-9 (tagged [guard] but its assertion FAILS at the merge-base — a guard holds there by definition; if this SPEC describes a change, tag it [change])
- GUARD_REGRESSED_AT_BASELINE SPEC-10 (tagged [guard] but its assertion FAILS at the merge-base — a guard holds there by definition; if this SPEC describes a change, tag it [change])
- GUARD_REGRESSED_AT_BASELINE SPEC-11 (tagged [guard] but its assertion FAILS at the merge-base — a guard holds there by definition; if this SPEC describes a change, tag it [change])
- GUARD_REGRESSED_AT_BASELINE SPEC-12 (tagged [guard] but its assertion FAILS at the merge-base — a guard holds there by definition; if this SPEC describes a change, tag it [change])
- GUARD_REGRESSED_AT_BASELINE SPEC-13 (tagged [guard] but its assertion FAILS at the merge-base — a guard holds there by definition; if this SPEC describes a change, tag it [change])
- GUARD_REGRESSED_AT_BASELINE SPEC-14 (tagged [guard] but its assertion FAILS at the merge-base — a guard holds there by definition; if this SPEC describes a change, tag it [change])

## Guard baseline coverage

- 14 [guard] SPEC(s) declared; 3 verified at the merge-base, 11 failed.
