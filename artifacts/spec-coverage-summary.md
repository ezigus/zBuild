## spec-coverage — covered

- Every checkbox requirement in the issue maps to at least one SPEC — v2 result on all exit paths (SPEC-1–4,18), valid_verdicts (SPEC-7), no path construction in code (SPEC-14), router budgets in manifest (SPEC-11,12), golden diff (SPEC-15), primary output (SPEC-13), provides.events/role (SPEC-16,17), name-matched inputs (SPEC-20), and cleanup hook (SPEC-5).

- every requirement the issue states maps to a declared SPEC
