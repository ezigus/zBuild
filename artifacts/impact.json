{"schema_version":1,"verdict":"incomplete","missing":[{"step_id":"build-summary-schema","files_to_add":["tests/golden/build-summary-artifact.golden","tests/unit/plugin-artifact-goldens-test.sh"],"reason":"The golden file pins the exact JSON structure of build-summary.json (without result_contract, disposition, or reason fields), and the goldens test hardcodes the assertion schema_version == 4 plus an exact golden match. If issue #1833 makes result_contract:2 unconditionally present in the build-summary.json output (as the in-scope build-empty-diff-done-sentinel-test.sh fixtures already assume), both files break.","evidence":"tests/golden/build-summary-artifact.golden line 1 omits result_contract entirely; tests/unit/plugin-artifact-goldens-test.sh line 113 asserts .schema_version == 4 and lines 126-132 assert an exact golden match against that file."}]}
