## spec-coverage — covered

- Every acceptance checkbox maps to at least one SPEC — v2 result on all exit paths (SPEC-1–4), valid_verdicts declared and tested (SPEC-7 + SPEC-2/3), no hardcoded paths (SPEC-14), router budgets in manifest with override precedence (SPEC-11/12), passing-run behavior guarded (SPEC-9), primary output declared (SPEC-13), and cleanup hook declared (SPEC-5); the "npm test green" and "reddens at merge-base" checkboxes are process/TDD requirements, not functional ones, and need no SPEC entry.

- every requirement the issue states maps to a declared SPEC
