# Design incomplete — router timeout, re-iterating

Design did not complete (reason=router_timeout). No acceptance block is emitted, so the design-gate rejects this artifact and the design cycle re-iterates.
