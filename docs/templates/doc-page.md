<!-- doc-page.md: LLM output contract for plugin and mechanic wiki pages.
     The doc generator (issues #876/#1356) injects this template into the LLM
     prompt to enforce the required section structure and DOC-STYLE rules.
     SLOT sentinels (<!-- SLOT: NAME -->) mark required sections in order; the
     prose annotations beneath each one are authoring instructions for that slot.
     See docs/DOC-STYLE.md for the full prose standard. -->

# doc-page template

This template encodes the shared LLM output contract for every plugin and mechanic wiki page generated by zBuild's doc automation.

<!-- VARIANT: plugin | mechanic
     Select the page shape the generator emits. Plugin pages include manifest
     YAML inside WHAT_IT_DOES; mechanic pages describe engine behavior instead. -->

<!-- SLOT: NEWCOMER_OPENING -->
<!-- DOC-STYLE rules 1 + 5: Open for a total newcomer. Write one or two plain
     sentences (≥5 words, ending in . ! or ?) that tell a first-time reader
     what this plugin/mechanic is, who it is for, and what problem it solves —
     BEFORE any mechanism, list, or code block. No bold structural label
     ("Kind:", "Manifest:", etc.). No heading. Just plain prose. -->

<!-- SLOT: WHAT_IT_DOES -->
## What it does

<!-- DOC-STYLE rule 3: Progressive disclosure — mechanism details follow the
     newcomer overview. For plugin pages include the manifest YAML block here.
     For mechanic pages describe the engine behavior, wiring, and key behaviors
     in plain terms. -->

<!-- SLOT: EXAMPLE -->
## Example

<!-- DOC-STYLE rule 6: Provide a real, copy-pasteable example as early as it
     makes sense — a YAML snippet, CLI invocation, or config fragment that a
     reader can try immediately. -->

<!-- SLOT: ADVANCED -->
## Advanced

<!-- DOC-STYLE rule 4: Anything that only makes sense to someone already
     familiar with zBuild goes under a heading that starts with "Advanced".
     Open with a one-line note that newcomers can skip this section.
     Include: ADR references, engine internals, architectural constraints,
     expert-only config options, and cross-cutting wiring details. -->

_Newcomers can skip this section._

_See [[docs/DOC-STYLE.md]] and [[docs/ARCHITECTURE.md]] for the full standards._
