{"result_contract":2,"schema_version":1,"name":"sre","score":6,"findings":[{"file":"plugins/agent/security-lens/plugin.sh","category":"observability","severity":"high","line":null,"message":"The manifest declares `security_lens.failed` as an emitted event, but no `emit_event \"security_lens.failed\"` call appears anywhere in plugin.sh — all error paths emit only `plugin.result`, so any monitor or alert wired to `security_lens.failed` will never fire."},{"file":"plugins/agent/security-lens/plugin.sh","category":"observability","severity":"medium","line":null,"message":"The rc=130 interrupt path writes a v2 artifact but emits no event to the bus; event-bus consumers watching for `plugin.result` or `security_lens.failed` cannot distinguish an interrupted review from a review that never ran."},{"file":"plugins/agent/security-lens/plugin.sh","category":"observability","severity":"medium","line":60,"message":"`stub` is written as the JSON string `\"false\"` in `_security_lens_write_result`, whereas the pre-migration artifact carried the boolean `false`; consumers using `jq -e '.stub'` or `.data.stub` for truthiness checks will silently invert behavior (a string is truthy in jq, a boolean false is falsy)."},{"file":"plugins/agent/security-lens/plugin.sh","category":"failure-mode","severity":"medium","line":null,"message":"`_security_lens_interrupt_handler` calls `_security_lens_write_result` (which spawns `jq` and `atomic_write`) inside a signal trap; if a second signal arrives during the handler, `atomic_write`'s temp-file rename may be abandoned and the artifact left corrupt or absent."},{"file":"plugins/agent/security-lens/plugin.sh","category":"failure-mode","severity":"low","line":null,"message":"`_sl_out_ref` and `_sl_interrupted` are module-level (non-local) variables; if the shell sources plugin.sh more than once in the same process (e.g. test re-source after SPEC-10 shadow-and-restore), stale values from a prior interrupted run could suppress a required artifact write on the next run."},{"file":"core/pipeline/runner.sh","category":"failure-mode","severity":"low","line":1492,"message":"The cross-check guard now fires even when `ZBUILD_STATE_DIR` is explicitly set, meaning a corrupt but ultimately-ignored `ZBUILD_STATE_FILE` will abort the pipeline rather than being superseded by the authoritative state dir — the fail-closed behaviour is documented but widens blast radius for operators who set both variables in wrapper scripts."}],"verdict":"complete","disposition":"complete","reason":"lens review complete"}