Based on my investigation, the prior verdict of `complete` holds. All files outside the design scope that reference acceptance-gate-result.json, acceptance-summary.txt, or result_contract either use synthetic data and won't be affected by the changes, or only assert on fields (.verdict, .failures, .reason) that aren't changing. No file outside scope pins the `result_contract:2` field on spec-acceptance output, the `required:false→true` change for acceptance_detail, or the precondition_unmet/malformed paths now writing acceptance-summary.txt.
