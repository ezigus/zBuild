{"result_contract":2,"schema_version":1,"name":"correctness","score":7,"findings":[{"file":"plugins/agent/security-lens/plugin.sh","category":"logic","severity":"medium","line":62,"message":"The `stub` field is written as the JSON string `\"false\"` rather than the boolean `false` in `_security_lens_write_result` (both at the top level and inside `.data`); any jq consumer branching on `.stub` as a boolean (e.g. `if .stub then … end`) will receive a truthy value from the non-empty string, silently inverting the intended semantics — tests using `jq -r` do not catch this because `-r` renders both `false` and `\"false\"` as the output string `false`."},{"file":"plugins/agent/security-lens/plugin.sh","category":"logic","severity":"low","line":170,"message":"The `emit_event` call on the `resolve_tier` failure path hardcodes `\"router_rc=2\"` as a literal key-value string rather than referencing the `$router_rc` variable (which is `0` at that point in the function); consumers that parse `router_rc` as a diagnostic integer will see a sentinel value that conflates tier-resolution failure with a router exit-code-2 failure."},{"file":"plugins/agent/security-lens/plugin.sh","category":"logic","severity":"low","line":170,"message":"The `emit_event` on the `resolve_tier` fatal path does not include `result_contract=2`, while the normal-pass and router-fatal paths both emit that field; downstream consumers asserting that every `plugin.result` event from this plugin carries `result_contract=2` will see an inconsistent event on the tier-resolution failure branch."},{"file":"core/pipeline/runner.sh","category":"logic","severity":"low","line":1492,"message":"The JSON-validity cross-check at line ~1492 validates `ZBUILD_STATE_FILE` even when `ZBUILD_STATE_DIR` is also set and will override it in the assignment below; the validated file is never actually used in that scenario, so a corrupt ambient `ZBUILD_STATE_FILE` causes a hard error even though it has no effect on the chosen `state_file` path — this is intentional per the comment but is a fail-closed behaviour that will surprise callers who set both variables."}],"verdict":"complete","disposition":"complete","reason":"lens review complete"}