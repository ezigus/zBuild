{
  "schema_version": 1,
  "run_id": "20260913194621-2110",
  "stage": "build",
  "kind": "llm",
  "seq": 1,
  "input": "# Intent (advisory)\n# zBuild — Vision\n> The North Star for zBuild — it steers how zBuild is built. The README distills it; the wiki references it; every pipeline run is steered by it.\n## Intent\nzBuild turns a delivery process into data — templates and plugins — so software is built the same disciplined way every time. We build zBuild the way zBuild builds: a minimal core, with all behavior plugin-delivered and template-composed. A good change is small, aligned to the spec, proven by a test, and wired into the live path — never scaffolding that looks finished but does nothing.\n## Principles\n- **Target-agnostic by contract** — zBuild builds *any* repository, not itself; the engine and plugins never assume the target's language, layout, or that zBuild is the target. Target knowledge lives in data — templates, config, per-repo overrides — never in code.\n- **Spec wins over drift** — KEEPERS, ARCHITECTURE, and the ADRs are the source of truth; when code disagrees, the code changes, not the spec.\n- **Small engine, behavior at the edges** — keep the core tiny; add capability as plugins composed in templates, never by growing the engine.\n- **Safety is structural** — all model-bound text passes one redaction chokepoint; state is atomic and resumable; scope is governed at every boundary.\n- **Models are data, not code** — selection flows through the router and config tiers; never hardcode a model name.\n- **Prove behavior, then wire it** — every change ships a test that fails without it and connects to the live path; green-but-inert is a defect.\n- **Fail closed, never silent** — gates block on real problems and say why; bounded cycles and recovery drive work to done.\n- **Dogfood every change** — build zBuild with zBuild; if the pipeline can't ship it, fix the pipeline.\n\n============== ZBUILD BUILD — iter 1/10 ==============\n## ORIGINAL TASK (immutable across iterations)\n# Plan: Migrate review-report plugin to contract v2\n\n**Goal:** Adopt the v2 result contract (result_contract:2, verdict, disposition, reason), fix the bare extract_first_json_object lens parse defect, inject router budget guidance into lens prompts, and declare router budgets in the manifest — all within the review-report plugin boundary.\n\n## Steps\n1. Write failing tests (SPEC assertions) for all acceptance criteria before touching any implementation: (a) v2 result written on every exit path with result_contract/verdict/disposition/reason; (b) valid_verdicts enforced — each emittable verdict is in the declared set; (c) each lens prompt contains a budget-guidance block sourced from _route_resolve_max_turns/_route_resolve_timeout; (d) disposition:exhausted emitted when the router signals budget exhaustion; (e) _rr_parse_lens_out recovers via schema-gate rather than LAST-wins bare extract; (f) manifest declares result_contract:2 and router: block. Run the new SPECs and confirm each fails for the stated reason.\n   - Files: `tests/unit/review-report-plugin-test.sh`\n   - Estimated lines: 120\n2. Amend manifest.yaml: (1) add \\`result_contract: 2\\` under the provides: block; (2) add a \\`config.router:\\` section with \\`timeout_s\\` and \\`max_turns\\` drawn from the same ranges the engine enforces (timeout_s: 300, max_turns: 25 as defaults matching the lenses' T2 budget — template override still wins per manifest-router-budget.sh). The primary:true output already exists; record that in the plan notes. Run SPEC-f to confirm it now passes.\n   - Files: `plugins/agent/review-report/manifest.yaml`\n   - Estimated lines: 10\n3. Add \\`_rr_lens_envelope_schema_ok\\` predicate and route lenses.sh _rr_parse_lens_out through \\`_llm_envelope_parse --schema-gate _rr_lens_envelope_schema_ok\\`, mirroring the security-lens pattern (security-lens/plugin.sh:42 and :140). Source scripts/lib/llm-agent.sh in plugin.sh (before lenses.sh is sourced). Keep identical visible-failure behaviour: emit review_report.lens.unparseable and degrade to empty findings when the schema gate fails. Run SPEC-e to confirm the recovery test now passes.\n   - Files: `plugins/agent/review-report/lib/lenses.sh`, `plugins/agent/review-report/plugin.sh`\n   - Estimated lines: 30\n4. Add \\`_rr_budget_guidance <max_turns> <timeout_s>\\` helper to plugin.sh, mirroring \\`_design_budget_guidance\\` from plugins/agent/design/plugin.sh:64-90. Resolve values via \\`_route_resolve_max_turns\\` and \\`_route_resolve_timeout\\` in \\`_rr_run_inner\\` and inject the block into \\`_rr_build_lens_prompt\\` (or prepend it per-lens before writing the prompt file in _rr_fanout_lenses). Never hand-copy numbers — always call the router helpers so the block drifts with the enforced limit. Run SPEC-c and SPEC-d to confirm budget guidance and exhausted disposition pass.\n   - Files: `plugins/agent/review-report/plugin.sh`, `plugins/agent/review-report/lib/lenses.sh`\n   - Estimated lines: 45\n5. Write v2 result file on every terminal exit path in plugin.sh. Add \\`_rr_write_result <artifact_dir> <verdict> <disposition> <reason>\\` helper (pattern from teardown/plugin.sh:52-61 and design/plugin.sh:56-61). Call it at: (1) missing state_file → verdict=error, disposition=broken; (2) missing out_json → verdict=error, disposition=broken; (3) router exhausted → verdict=pass, disposition=exhausted; (4) normal completion → verdict=pass, disposition=complete. The result file path is \\`$artifact_dir/review-report-result.json\\` — distinct from the advisory \\`review-report.json\\`. Run SPEC-a and SPEC-b to confirm all exit paths produce a conformant v2 result.\n   - Files: `plugins/agent/review-report/plugin.sh`\n   - Estimated lines: 50\n6. Produce a before/after golden diff to confirm the passing-run output is unchanged. Run \\`npm test\\` with all changes committed (so the mutation tier engages per CLAUDE.md). Confirm: (1) all new SPECs are green; (2) existing review-report tests still pass; (3) \\`grep -n 'extract_first_json_object' plugins/agent/review-report/lib/lenses.sh\\` returns no match (path construction assertion); (4) coverage floor not broken.\n   - Files: `tests/golden/review-report-pass.json`\n   - Estimated lines: 25\n\n## Notes\nPRIMARY OUTPUT ALREADY DECLARED: review-report/manifest.yaml already has `primary: true` on the `review_report` output (line 85). No change needed for that acceptance criterion — the issue asks us to record that fact. VALID_VERDICTS ALREADY []  with a lint comment — the plugin is advisory and emits no verdict through the verdict channel (it returns 0 always); the manifest already states `valid_verdicts: []` with an explicit claim comment. The issue says a verdict outside the declared set is a structural failure; because the plugin emits no verdict at all, the empty set is correct and no change is needed to that field specifically. RESULT FILE vs ADVISORY REPORT: the v2 result file (step-5) is the engine-readable contract artifact (result_contract/verdict/disposition/reason); the existing `review-report.json` is the advisory human-readable report. They are separate files. The output manifest should gain a new output entry for `review-report-result.json` with `required:true` (covered in step-5's manifest amendment). INPUTS: the issue's name-matched input migration (no `from:`, no path construction in code) appears already satisfied — the manifest uses `id:` only with no path or producer fields, and the plugin derives paths from state_dir rather than manifest paths. A grep assertion in step-6 will verify no hardcoded artifact paths remain. COORDINATE WITH #1719: review-summary comment count / verdict marker — the issue flags this but grants no scope to fix it here. Record as a known gap: this migration does not alter the PR-comment behaviour beyond existing fail-soft `gh pr comment`. CLEANUP HOOK: the plugin holds no live resources (no open sockets, no mounted volumes); the `cleanup` hook is legitimately absent. This must be recorded in the manifest with a comment, not implied by silence, per the issue's cleanup requirement (#1829). Add a `hooks:` comment or a `cleanup: ~` null entry in manifest.yaml in step-2. LENSES RUN IN SUBSHELLS: the budget-guidance injection in step-4 must write the budget into the per-lens prompt file (built sequentially before subshell launch) rather than relying on a subshell-visible variable, because subshells inherit env but not function calls on the hot path. The prompt file approach already used in _rr_fanout_lenses handles this naturally. GOLDEN DIFF: a golden file may not yet exist; step-6 may need to create it. The before-state must be captured from merge-base. If the golden dir has no existing snapshot this step produces the baseline rather than diffs it — record as a conditional.\n\n## INSTRUCTIONS\nReason about correctness first: a change is done when every assertion in the acceptance suite passes, edge cases are handled, and the implementation does exactly what the plan says, no more and no less. Prefer the simplest implementation that satisfies the acceptance contract, treat every failing test as an obligation to understand before touching anything else, and flag any gap between the stated task and what the code actually achieves.\n\nYou have Read, Edit, Write, and\nBash tools available. Your job is to edit the working tree to implement the\nORIGINAL TASK above.\n\n### Scope (plan.files[])\nYou may ONLY touch files listed here. Refuse any out-of-scope edit.\n  - plugins/agent/review-report/manifest.yaml\n  - plugins/agent/review-report/plugin.sh\n  - plugins/agent/review-report/lib/lenses.sh\n  - tests/unit/review-report-plugin-test.sh\n  - tests/golden/review-report-pass.json\n  - tests/integration/review-report-advisory-flow-test.sh\n  - tests/unit/call-graph-evidence-test.sh\n  - tests/unit/review-lens-report-merge-base-bundle-test.sh\n  - tests/unit/review-aggregator-test.sh\n  - tests/unit/tier-resolve-test.sh\n  - tests/unit/pr-open-advisory-review-test.sh\n  - tests/unit/event-schema-emitted-coverage-test.sh\n  - tests/unit/plugin-route-source-guard-test.sh\n  - docs/wiki/plugins/review-report.md\n  - docs/adr/ADR-038-adversarial-multilens-review-report.md\n  - docs/adr/ADR-054-stage-contract.md\n  - docs/adr/ADR-063-budget-disclosure-and-partial-output.md\n  - docs/adr/ADR-028-shared-llm-agent-framework.md\n  - docs/adr/ADR-055-inter-stage-data-contract-v2.md\n  - scripts/lib/llm-agent.sh\n  - core/contract/version.sh\n\n### How the loop works\n- Each iteration you may make code changes via Edit/Write/Bash.\n- After each iteration the pipeline captures `git diff HEAD` and feeds it\n  back to you so you can verify progress.\n- Do NOT emit a unified diff in your response — the pipeline derives the\n  canonical `diff.patch` artifact from `git diff HEAD` automatically.\n\n### Completion sentinel\nEmit `LOOP_COMPLETE` on its own line as the FINAL line of your response\nWHEN the implementation is complete — whether you just finished it OR\nit was already done before you started. If the branch already contains\nthe required changes (check `git log` for commits + `git diff` for any\nremaining gap), emit `LOOP_COMPLETE` immediately. Do NOT keep iterating\nwhen there is nothing left to do.\n\n### Rules\n- Touch only files in the scope list above.\n- Do not run `git commit` — the pipeline owns commit semantics.\n- NEVER run a command that mutates git branch state or the working tree —\n  no `git checkout -b`/`switch`/`commit`/`push`/`tag`/`reset`. You are running\n  INSIDE the pipeline's own checkout; switching or creating a branch hijacks the\n  run. The pipeline owns the branch.\n- If you must exercise a command you are building (or any command that could\n  publish, tag, push, or otherwise mutate state), run it in `--dry-run` mode.\n  Do everything you can to verify behavior without side effects.\n- Keep changes minimal and aligned with the plan.\n- Clean up scratch siblings of the files you edit (`*.bak`, `*.orig`,\n  `*.rej`, `*.head`, `*.tmp`, `*~`). `sed -i.bak` and\n  `git show HEAD:f > f.head` both leave one behind — prefer `sed -i` with no\n  suffix, and delete any comparison scratch before you finish. Out-of-scope\n  scratch is removed for you; any OTHER out-of-scope path voids the whole\n  iteration, so do not rely on the cleanup.\n- If satisfying a gate requires a file outside your scope, do not work around it — emit `BLOCKED: <gate> requires <file> (out of scope)` and stop.\n\n### Commit message (#608)\nBefore the final `LOOP_COMPLETE` line, emit a single line of the form:\n\n    COMMIT_SUMMARY: <one-line description of this iteration's change>\n\nKeep it under 72 characters, present tense, imperative mood (e.g.\n\"add foo parser\" not \"added\"). The pipeline uses this as the git commit\nmessage for the per-iteration commit it creates on your behalf. If you\nomit this line the pipeline falls back to the plan title.\n\n## DESIGN DECISIONS (from the design stage — honor these directives; they refine the plan)\n# Design: Migrate review-report plugin to contract v2 (#1843)\n\n## Architectural decision summary\n\n**Goal.** Bring `plugins/agent/review-report` up to the ADR-054 v2 result contract: write a `review-report-result.json` sidecar on every terminal exit path, declare `result_contract: 2` and a `config.router:` block in the manifest, replace the bare `extract_first_json_object` lens-parse path with the schema-gated `_llm_envelope_parse` recovery path, inject router budget guidance into each lens prompt, add `ZBUILD_STAGE_INPUTS` reading for all name-matched inputs, and emit `disposition: exhausted` when lens subshells signal budget exhaustion.\n\n**Context.** Twenty-five plugins are migrating one at a time (#1833–#1849). The engine already reads both v1 and v2 results (`_ZBUILD_CONTRACT_MIN=1`, `_ZBUILD_CONTRACT_MAX=2` in `core/contract/version.sh`). The review-report plugin currently: (1) never writes a sidecar result file; (2) uses bare `extract_first_json_object` in `_rr_parse_lens_out` (LAST-wins, no schema recovery); (3) never injects budget guidance; (4) does not source `scripts/lib/llm-agent.sh`; (5) constructs the scope_manifest path as `$state_dir/scope-manifest.md` rather than reading from `ZBUILD_STAGE_INPUTS`; (6) does not read plan, diff_patch, or intake_goal from `ZBUILD_STAGE_INPUTS`. The manifest already has `valid_verdicts: []`, `primary: true`, `provides.role`, `provides.events`, and no `cleanup` hook — those become guard SPECs only.\n\n**Decision.** Implement all sub-changes within the plugin boundary; no engine or template changes required. Pattern sources: `_design_write_result` and `_design_budget_guidance` from `plugins/agent/design/plugin.sh`; `_llm_envelope_parse --schema-gate` from `plugins/agent/security-lens/plugin.sh:140–148`. Result sidecar is `$artifact_dir/review-report-result.json` — distinct from the advisory `review-report.json`. All four declared inputs (scope_manifest, plan, diff_patch, intake_goal) are read from `ZBUILD_STAGE_INPUTS` when available, falling back to state_dir construction; after the change, plugin.sh must contain no hardcoded state_dir path construction for any declared input (blanket grep-asserted as SPEC-17). For `disposition: exhausted` (ADR-063 §3): track a `_rr_any_lens_failed` flag across the fan-out; if any lens subshell returns non-zero rc, write `verdict=pass, disposition=exhausted` instead of `disposition=complete` (advisory verdict stays pass). No `cleanup` hook is declared or implemented — the engine emits `plugin.cleanup.absent` by ADR-001 §2 contract.\n\n---\n\n\n---\n\n\nLOOP_COMPLETE\nWhere a design decision above conflicts with the plan, follow the design decision.\n\n## ACCEPTANCE TESTS (you MUST make these pass)\nYou do NOT author or modify acceptance assertions. They were written from the design contract by a separate stage, before your implementation existed, and they are the contract you implement against. A failing assertion means YOUR CODE is wrong — fix the code. You MUST NOT weaken, delete, retag or re-author any assertion, and you must not edit the testfiles listed below; the spawn denies it and a mechanical guard checks it. If you believe an assertion genuinely does not test its SPEC, say so in your output and leave it alone: correcting it is the author stage's job, not yours (#2022, ADR-036).\nEach test MUST contain an assert call whose label includes the [SPEC-n] tag for the SPEC it verifies (e.g. assert_eq \"[SPEC-1] ...\" exp act). The acceptance-gate (ADR-036) requires every SPEC-n to have a [SPEC-n]-tagged assertion. A CHANGE-behavior SPEC-n MUST have a tagged assertion that FAILS at the merge-base baseline and passes here (a tautological change-SPEC that passes without your implementation is rejected); a GUARD/invariant SPEC-n is tagged but NOT contorted to fail at baseline. See the per-id list below.\n- SPEC-1: tests/unit/review-report-plugin-test.sh\n- SPEC-2: tests/unit/review-report-plugin-test.sh\n- SPEC-3: tests/unit/review-report-plugin-test.sh\n- SPEC-4: tests/unit/review-report-plugin-test.sh\n- SPEC-5: tests/unit/review-report-plugin-test.sh\n- SPEC-6: tests/unit/review-report-plugin-test.sh\n- SPEC-7: tests/unit/review-report-plugin-test.sh\n- SPEC-8: tests/unit/review-report-plugin-test.sh\n- SPEC-9: tests/unit/review-report-plugin-test.sh\n- SPEC-10: tests/unit/review-report-plugin-test.sh\n- SPEC-11: tests/unit/review-report-plugin-test.sh\n- SPEC-12: tests/unit/review-report-plugin-test.sh\n- SPEC-13: tests/unit/review-report-plugin-test.sh\n- SPEC-14: tests/unit/review-report-plugin-test.sh\n- SPEC-15: tests/unit/review-report-plugin-test.sh\n- SPEC-16: tests/unit/review-report-plugin-test.sh\n- SPEC-17: tests/unit/review-report-plugin-test.sh\n\nWhat each SPEC requires:\n- [SPEC-1] manifest declares result_contract:2 under provides: and a config.router: block with timeout_s and max_turns\n- [SPEC-2] _rr_write_result helper exists in plugin.sh and writes a conformant v2 result file (result_contract:2, verdict, disposition, reason) at $artifact_dir/review-report-result.json\n- [SPEC-3] v2 result file written with verdict=error and disposition=broken on the missing state_file exit path\n- [SPEC-4] v2 result file written with verdict=error and disposition=broken on the missing out_json exit path\n- [SPEC-5] v2 result file written with verdict=pass and disposition=complete on normal completion (all lens subshells return rc=0)\n- [SPEC-6] _rr_budget_guidance helper exists in plugin.sh and injects a TURN BUDGET block sourced from _route_resolve_max_turns and _route_resolve_timeout into each lens prompt\n- [SPEC-7] _rr_lens_envelope_schema_ok predicate exists and validates {score:number, findings:array} shape\n- [SPEC-8] _rr_parse_lens_out routes through _llm_envelope_parse --schema-gate _rr_lens_envelope_schema_ok instead of bare extract_first_json_object; extract_first_json_object no longer appears in lenses.sh\n- [SPEC-9] _rr_run_inner returns 0 and advisory review-report.json is still written on normal completion (advisory contract preserved)\n- [SPEC-10] 11 independent LLM calls are still made (one per lens) and findings are still aggregated and de-duped\n- [SPEC-11] manifest declares valid_verdicts: [] (advisory — writes no verdict to the engine's pipeline verdict channel)\n- [SPEC-12] manifest outputs first entry (review_report) retains primary: true after migration\n- [SPEC-13] manifest provides.role: review_report and all four declared provides.events are present after migration\n- [SPEC-14] no cleanup hook key in manifest.hooks and no review_report_cleanup function in plugin.sh (cleanup absent-and-recorded: engine emits plugin.cleanup.absent by ADR-001 §2)\n- [SPEC-15] review_report_run reads scope_manifest input path from ZBUILD_STAGE_INPUTS index when available, falling back to $state_dir/scope-manifest.md; the plugin calls jq -r '.inputs.scope_manifest' \"$ZBUILD_STAGE_INPUTS\" for this input\n- [SPEC-16] v2 result sidecar written with verdict=pass and disposition=exhausted when at least one lens subshell returns non-zero rc (partial advisory results signalled per ADR-063 §3); verdict stays pass to preserve advisory contract\n- [SPEC-17] plugin.sh constructs no hardcoded artifact path from state_dir for any of the four declared input ids (scope_manifest, plan, diff_patch, intake_goal) — a blanket grep over plugin.sh confirms no occurrence of state_dir concatenated with any declared input filename (scope-manifest.md, plan.json, diff.patch, intake.md, intake-goal.md); all inputs are sourced exclusively from ZBUILD_STAGE_INPUTS when the index is present\n\n### SPEC IDS YOU MUST COVER (acceptance gate, ADR-036)\nEvery SPEC id below needs at least one assertion whose label carries its [SPEC-n] tag; self-verify the FULL set is tagged before emitting LOOP_COMPLETE. A CHANGE-behavior SPEC (new behavior this change introduces) MUST have a [SPEC-n] assertion that FAILS at the merge-base baseline. A GUARD/invariant SPEC (behavior that must stay unchanged) is tagged but MUST NOT be contorted to fail at baseline.\n- [SPEC-1] manifest declares result_contract:2 under provides: and a config.router: block with timeout_s and max_turns\n  → needs a `[SPEC-1]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-2] _rr_write_result helper exists in plugin.sh and writes a conformant v2 result file (result_contract:2, verdict, disposition, reason) at $artifact_dir/review-report-result.json\n  → needs a `[SPEC-2]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-3] v2 result file written with verdict=error and disposition=broken on the missing state_file exit path\n  → needs a `[SPEC-3]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-4] v2 result file written with verdict=error and disposition=broken on the missing out_json exit path\n  → needs a `[SPEC-4]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-5] v2 result file written with verdict=pass and disposition=complete on normal completion (all lens subshells return rc=0)\n  → needs a `[SPEC-5]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-6] _rr_budget_guidance helper exists in plugin.sh and injects a TURN BUDGET block sourced from _route_resolve_max_turns and _route_resolve_timeout into each lens prompt\n  → needs a `[SPEC-6]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-7] _rr_lens_envelope_schema_ok predicate exists and validates {score:number, findings:array} shape\n  → needs a `[SPEC-7]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-8] _rr_parse_lens_out routes through _llm_envelope_parse --schema-gate _rr_lens_envelope_schema_ok instead of bare extract_first_json_object; extract_first_json_object no longer appears in lenses.sh\n  → needs a `[SPEC-8]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-9] _rr_run_inner returns 0 and advisory review-report.json is still written on normal completion (advisory contract preserved)\n  → needs a `[SPEC-9]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-10] 11 independent LLM calls are still made (one per lens) and findings are still aggregated and de-duped\n  → needs a `[SPEC-10]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-11] manifest declares valid_verdicts: [] (advisory — writes no verdict to the engine's pipeline verdict channel)\n  → needs a `[SPEC-11]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-12] manifest outputs first entry (review_report) retains primary: true after migration\n  → needs a `[SPEC-12]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-13] manifest provides.role: review_report and all four declared provides.events are present after migration\n  → needs a `[SPEC-13]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-14] no cleanup hook key in manifest.hooks and no review_report_cleanup function in plugin.sh (cleanup absent-and-recorded: engine emits plugin.cleanup.absent by ADR-001 §2)\n  → needs a `[SPEC-14]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-15] review_report_run reads scope_manifest input path from ZBUILD_STAGE_INPUTS index when available, falling back to $state_dir/scope-manifest.md; the plugin calls jq -r '.inputs.scope_manifest' \"$ZBUILD_STAGE_INPUTS\" for this input\n  → needs a `[SPEC-15]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-16] v2 result sidecar written with verdict=pass and disposition=exhausted when at least one lens subshell returns non-zero rc (partial advisory results signalled per ADR-063 §3); verdict stays pass to preserve advisory contract\n  → needs a `[SPEC-16]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n- [SPEC-17] plugin.sh constructs no hardcoded artifact path from state_dir for any of the four declared input ids (scope_manifest, plan, diff_patch, intake_goal) — a blanket grep over plugin.sh confirms no occurrence of state_dir concatenated with any declared input filename (scope-manifest.md, plan.json, diff.patch, intake.md, intake-goal.md); all inputs are sourced exclusively from ZBUILD_STAGE_INPUTS when the index is present\n  → needs a `[SPEC-17]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)\n\n## Iteration 1/10\n(No prior changes — this is the first iteration.)\n\n## BRANCH STATE since intake (HEAD: 998b2b8b)\nCommits:\n  (none)\n\nDiff vs intake baseline:\n  (no changes)\n\n\n## STAGE INPUTS (engine-resolved)\n\nThe engine resolved every input this stage declared. These are the\nliteral paths — read them with your normal file tools. Do NOT guess a\nfilename, and do NOT search the tree for these artifacts.\n\n  scope_manifest: /home/runner/work/_temp/zbuild-state/scope-manifest.md\n  intake_baseline_ref: /home/runner/work/_temp/zbuild-state/intake-baseline-ref.txt\n  plan: /home/runner/work/_temp/zbuild-state/artifacts/plan.json\n  design: /home/runner/work/_temp/zbuild-state/artifacts/design.md\n\nA path listed here may not exist yet if its producer declared it\noptional; treat an absent optional input as empty.\n\n\n## STAGE SUMMARIES (engine-collected)\n\nWhat each completed stage reported, newest content per stage. A stage\nmarked RESOLVE blocks convergence — address its findings before you\nfinish. The rest is context.\n\n### hydrate (verdict: pass)\n## hydrate — complete\n\n- restored 0 artifact(s) from prior runs (empty)\n\n- source: none\n- detail: no prior work for issue 1843 (first run)\n### intake (verdict: pass)\n## intake — pass\n\n- took in the goal for issue #1843: [Phase 0/F] migrate the review-report plugin to contract v2\n\n- goal length: 5730 chars\n- platforms: 1\n### plan (verdict: pass)\n## plan — pass\n\n- decomposed the goal into 6 step(s)\n\n- scope violations: 0\n- DoD discipline: 1\n- artifact: plan.json\n### design (verdict: pass)\n## design — pass\n\n- authored design.md — 21 file(s) in scope, 34 acceptance SPEC(s)\n\n- scope: plugins/agent/review-report/manifest.yaml,plugins/agent/review-report/plugin.sh,plugins/agent/review-report/lib/lenses.sh,tests/unit/review-report-plugin-test.sh,tests/golden/review-report-pass.json,tests/integration/review-report-advisory-flow-test.sh,tests/unit/call-graph-evidence-test.sh,tests/unit/review-lens-report-merge-base-bundle-test.sh,tests/unit/review-aggregator-test.sh,tests/unit/tier-resolve-test.sh,tests/unit/pr-open-advisory-review-test.sh,tests/unit/event-schema-emitted-coverage-test.sh,tests/unit/plugin-route-source-guard-test.sh,docs/wiki/plugins/review-report.md,docs/adr/ADR-038-adversarial-multilens-review-report.md,docs/adr/ADR-054-stage-contract.md,docs/adr/ADR-063-budget-disclosure-and-partial-output.md,docs/adr/ADR-028-shared-llm-agent-framework.md,docs/adr/ADR-055-inter-stage-data-contract-v2.md,scripts/lib/llm-agent.sh,core/contract/version.sh\n- artifact: design.md\n### spec-coverage (verdict: pass)\n## spec-coverage — covered\n\n- SPEC-17 is the blanket grep assertion the issue requires — it explicitly covers all four declared input ids (scope_manifest, plan, diff_patch, intake_goal) by asserting no occurrence of `state_dir` concatenated with any declared input filename in plugin.sh; the spec-coverage stage's finding incorrectly treated SPEC-15 as the only relevant spec and did not account for SPEC-17.\n\n- every requirement the issue states maps to a declared SPEC\n### impact (verdict: warn)\n## impact — pass\n\n- assessed the change against the design\n\n- artifact: impact.json\n### test-author (verdict: fail) — RESOLVE these findings before completing\n## test-author — degraded\n\n- the model call failed (router_timeout) — no assertions were authored\n\n- SPECs in the contract: 17\n- assertions are authored from the SPEC text, never from the implementation\n### spec-correspondence (verdict: fail) — RESOLVE these findings before completing\n## spec-correspondence — mismatch\n\n- judged 1 SPEC(s): 0 correspond, 0 partial, 1 mismatch, 0 uncheckable, 0 unjudged\n\n- SPEC-1 MISMATCH: The requirement is about static manifest declarations (`result_contract:2` under `provides:` and a `config.router:` block with `timeout_s` and `max_turns`), while the assertion tests runtime behavior — exit code, LLM call count, and output report structure — none of which establish what the manifest file declares.",
  "output": "",
  "exit_code": 124,
  "duration_ms": 900801,
  "metadata": {
    "tier": "T2",
    "iter": "1",
    "model_id": "claude-sonnet-4-6",
    "error": "true"
  },
  "ts": "2026-09-13T21:00:53Z"
}
