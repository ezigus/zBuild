<!-- doc-page.md: LLM output contract for plugin and mechanic wiki pages.
     The doc generator (issues #876/#1356) injects this template into the LLM
     prompt to enforce the required section structure and DOC-STYLE rules.
     SLOT sentinels (<!-- SLOT: NAME -->) mark the required sections in order; the
     prose annotations beneath each one are authoring instructions for that slot.
     See docs/DOC-STYLE.md for the full prose standard. -->

# doc-page template

This template encodes the shared LLM output contract for every plugin and mechanic wiki page generated by zBuild's doc automation.

<!-- VARIANT: plugin | mechanic
     Select the page shape the generator emits. For a plugin the fields come from
     its manifest.yaml (summary, usage, kind, hooks, requires, inputs, outputs);
     for a mechanic they come from config/mechanics.yaml (summary, usage,
     defined_in). The section structure below is identical for both variants. -->

<!-- SLOT: NEWCOMER_OPENING -->
<!-- Source: manifest/registry `summary` (inferred from the code when absent).
     DOC-STYLE rules 1 + 5: Open for a total newcomer. Write one or two plain
     sentences (>= 5 words, ending in . ! or ?) that tell a first-time reader what
     this plugin/mechanic is, who it is for, and what problem it solves — BEFORE
     any mechanism, list, or code block. No bold structural label ("Kind:",
     "Manifest:"). No heading. Just plain prose. -->

<!-- SLOT: HOW_TO_USE -->
## How to use

<!-- Source: manifest/registry `usage`. DOC-STYLE rule 6: show how a reader
     actually invokes or composes this — the CLI command, the template `flow:`
     entry, or the config fragment they can copy and run immediately. -->

<!-- SLOT: REFERENCE -->
## Reference

<!-- Source: the manifest/registry FIELDS themselves (progressive disclosure,
     DOC-STYLE rule 3). Enumerate the structured facts a reader looks up:
     for a plugin — kind, hooks, requires, inputs, outputs, tier_default (the
     manifest.yaml block); for a mechanic — defined_in and its key behaviors. -->

<!-- SLOT: ADVANCED -->
## Advanced

<!-- DOC-STYLE rule 4: Anything that only makes sense to someone already familiar
     with zBuild goes under a heading that starts with "Advanced". Open with a
     one-line note that newcomers can skip this section. Include: ADR references,
     engine internals, architectural constraints, and expert-only options. -->

_Newcomers can skip this section._

_See docs/DOC-STYLE.md for the full prose standard._
