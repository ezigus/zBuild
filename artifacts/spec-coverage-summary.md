## spec-coverage — covered

- Every checkbox from the issue's acceptance list maps 1-to-1 onto a SPEC (SPEC-1 through SPEC-20), including v2 result on all exit paths, valid_verdicts, no artifact paths in code, router budgets, golden diff, primary output, name-matched inputs, provides.events/role, interrupt handler, and cleanup function without manifest declaration.

- every requirement the issue states maps to a declared SPEC
