============== ZBUILD BUILD — iter 5/10 ==============
## ORIGINAL TASK (immutable across iterations)
# Plan: Migrate spec-acceptance to contract v2

**Goal:** Adopt the v2 result contract (result_contract:2, disposition, valid_verdicts enforcement, name-matched inputs, required summary on every terminal path) in plugins/agent/spec-acceptance, leaving all other behaviour unchanged.

## Steps
1. Write the failing contract test first (TDD red step). New file asserts: (a) result_contract:2 present on all three exit paths (precondition_unmet, malformed_acceptance_block, pass, fail); (b) acceptance-summary.txt exists on every terminal verdict including precondition_unmet and malformed paths; (c) grep over plugin.sh produces zero hits for the literal '$artifact_dir/design.md' construction. Run the test to confirm it fails at merge-base for each assertion — record the red output in the PR body.
   - Files: `tests/integration/acceptance-gate-v2-contract-test.sh`
   - Estimated lines: 110
2. Update the manifest. Add \`result_contract: 2\` under \`provides:\`. Change \`acceptance_detail\` from \`required: false\` to \`required: true\` and remove the comment that justified the false value (the comment directly contradicts ADR-055 §9). No other manifest fields change — role, events, valid_verdicts, primary, tier_default, cleanup absence are all already correct.
   - Files: `plugins/agent/spec-acceptance/manifest.yaml`
   - Estimated lines: 8
3. Update plugin.sh with four focused changes. (1) Input resolution: replace the hardcoded \`local design_md="$artifact_dir/design.md"\` (line 239) with ZBUILD_STAGE_INPUTS JSON lookup (\`jq -r '.inputs.design // empty' "$ZBUILD_STAGE_INPUTS"\`) following the pattern in plugins/agent/design/plugin.sh:397-398. (2) Add \`result_contract:2\` to every jq result emission: _ag_noop_precondition_unmet (line 181), the malformed-block write (line 264), and both branches of the final write (lines 549-557). (3) Write acceptance-summary.txt on the precondition_unmet path (stating verdict=pass and reason) and on the malformed path (stating verdict=fail and reason) so the artifact exists on every terminal path. (4) Remove the \`|| true\` from the summary write at line 538 so a failed write is not silently swallowed.
   - Files: `plugins/agent/spec-acceptance/plugin.sh`
   - Estimated lines: 40
4. Update the five existing test helper functions that copy design.md to artifacts/ without setting ZBUILD_STAGE_INPUTS. For each, write a temp JSON file \`{"inputs":{"design":"<path>"}}\` and export ZBUILD_STAGE_INPUTS pointing to it, alongside or replacing the existing cp. The five locations are: acceptance-gate-test.sh lines 45 and 66 (_run_gate_with_summary and _run_gate), acceptance-gate-quiet-test.sh line 152, acceptance-gate-inert-wiring-iter1-test.sh line 46, acceptance-gate-reachability-test.sh line 32, acceptance-guard-regressed-routes-design-test.sh line 84.
   - Files: `tests/integration/acceptance-gate-test.sh`, `tests/integration/acceptance-gate-quiet-test.sh`, `tests/integration/acceptance-gate-inert-wiring-iter1-test.sh`, `tests/integration/acceptance-gate-reachability-test.sh`, `tests/integration/acceptance-guard-regressed-routes-design-test.sh`
   - Estimated lines: 50
5. Run \`npm test\` with the full tree staged (so the mutation tier engages per the DoD). Confirm all acceptance-gate tests and the new v2-contract test are green. If any existing test fails due to the result_contract field being unexpected, update only its assertion to accept the new field — do not weaken the structural assertions. Record the green run.
   - Estimated lines: 0

## Notes
Router budgets (DoD item): `tier_default: T1` is already declared in the manifest. This plugin is a mechanical (non-LLM) gate and never calls model.route, so there is no in-code budget resolution to migrate. The item is satisfied by the existing manifest entry — noted as N/A rather than a code change.

`primary: true` (DoD item): already declared on gate_result — no change needed.

`provides.role` and `provides.events` (DoD items): already declared — no change needed.

`cleanup` hook (DoD item): plugin holds no live resources; hook is intentionally absent. ADR-055 says this should be recorded, not implied — the absence is correct and is confirmed by inspection.

Summary on all terminal paths: the _ag_noop_precondition_unmet function is called on two code paths (design_acceptance_block and merge_base_resolvable preconditions). Both need a summary write. The summary content for a precondition_unmet path should state verdict=pass and the precondition id, consistent with ADR-055 §9 ('a summary states what the stage DID').

The `|| true` removal on the summary write (plugin.sh:538): given the `set -euo pipefail` environment, a bare atomic_write failure with no `|| true` will propagate. This is the correct behaviour — a failed summary write is a genuine error, not a silent skip. If the directory does not exist, mkdir -p runs first (already on line 536); the `|| true` there can remain.

Backwards compatibility: `failures` and `fault` remain at the top level of the result JSON (not moved under `data:`), because plugins/agent/build/lib/context.sh and core/pipeline/verdict.sh read them there. The issue explicitly states 'nothing else has to move with it', so the gate's existing public contract fields are untouched.

Golden diff (DoD item): a before/after snapshot of the acceptance-gate result and summary on a passing run should be captured as part of step-1 or step-5 to satisfy this checklist item — the new contract test in step-1 effectively serves this role by asserting field-by-field presence rather than a file diff.

The harness-hermeticity test does not reference design.md and requires no update.

## INSTRUCTIONS
Reason about correctness first: a change is done when every assertion in the acceptance suite passes, edge cases are handled, and the implementation does exactly what the plan says, no more and no less. Prefer the simplest implementation that satisfies the acceptance contract, treat every failing test as an obligation to understand before touching anything else, and flag any gap between the stated task and what the code actually achieves.

You have Read, Edit, Write, and
Bash tools available. Your job is to edit the working tree to implement the
ORIGINAL TASK above.

### Scope (plan.files[])
You may ONLY touch files listed here. Refuse any out-of-scope edit.
  - plugins/agent/spec-acceptance/manifest.yaml
  - plugins/agent/spec-acceptance/plugin.sh
  - tests/integration/acceptance-gate-v2-contract-test.sh
  - tests/integration/acceptance-gate-test.sh
  - tests/integration/acceptance-gate-quiet-test.sh
  - tests/integration/acceptance-gate-inert-wiring-iter1-test.sh
  - tests/integration/acceptance-gate-reachability-test.sh
  - tests/integration/acceptance-guard-regressed-routes-design-test.sh
  - tests/unit/summary-producers-test.sh
  - docs/adr/ADR-036-acceptance-contract-teeth.md
  - docs/adr/ADR-054-stage-contract.md
  - docs/adr/ADR-055-inter-stage-data-contract-v2.md
  - docs/wiki/plugins/spec-acceptance.md

### How the loop works
- Each iteration you may make code changes via Edit/Write/Bash.
- After each iteration the pipeline captures `git diff HEAD` and feeds it
  back to you so you can verify progress.
- Do NOT emit a unified diff in your response — the pipeline derives the
  canonical `diff.patch` artifact from `git diff HEAD` automatically.

### Completion sentinel
Emit `LOOP_COMPLETE` on its own line as the FINAL line of your response
WHEN the implementation is complete — whether you just finished it OR
it was already done before you started. If the branch already contains
the required changes (check `git log` for commits + `git diff` for any
remaining gap), emit `LOOP_COMPLETE` immediately. Do NOT keep iterating
when there is nothing left to do.

### Rules
- Touch only files in the scope list above.
- Do not run `git commit` — the pipeline owns commit semantics.
- NEVER run a command that mutates git branch state or the working tree —
  no `git checkout -b`/`switch`/`commit`/`push`/`tag`/`reset`. You are running
  INSIDE the pipeline's own checkout; switching or creating a branch hijacks the
  run. The pipeline owns the branch.
- If you must exercise a command you are building (or any command that could
  publish, tag, push, or otherwise mutate state), run it in `--dry-run` mode.
  Do everything you can to verify behavior without side effects.
- Keep changes minimal and aligned with the plan.
- Clean up scratch siblings of the files you edit (`*.bak`, `*.orig`,
  `*.rej`, `*.head`, `*.tmp`, `*~`). `sed -i.bak` and
  `git show HEAD:f > f.head` both leave one behind — prefer `sed -i` with no
  suffix, and delete any comparison scratch before you finish. Out-of-scope
  scratch is removed for you; any OTHER out-of-scope path voids the whole
  iteration, so do not rely on the cleanup.
- If satisfying a gate requires a file outside your scope, do not work around it — emit `BLOCKED: <gate> requires <file> (out of scope)` and stop.

### Commit message (#608)
Before the final `LOOP_COMPLETE` line, emit a single line of the form:

    COMMIT_SUMMARY: <one-line description of this iteration's change>

Keep it under 72 characters, present tense, imperative mood (e.g.
"add foo parser" not "added"). The pipeline uses this as the git commit
message for the per-iteration commit it creates on your behalf. If you
omit this line the pipeline falls back to the plan title.

## DESIGN DECISIONS (from the design stage — honor these directives; they refine the plan)
# Design — Migrate spec-acceptance to contract v2

## Architectural Decision Summary

**Goal.** Adopt the ADR-055 v2 inter-stage data contract in `plugins/agent/spec-acceptance`,
bringing it into parity with the other F-wave plugin migrations (#1833–#1849). The gate's
observable behavior — verdict semantics, disposition classification, event emission, negctl/
reachability levels — is unchanged; only the result file envelope, input-resolution pattern,
and summary-completeness move.

**Context.** The v2 contract requires four things spec-acceptance currently lacks:
(1) `result_contract:2` in the emitted JSON on every exit path;
(2) a `disposition` field on the precondition_unmet and malformed paths (already present on the
    main pass/fail path);
(3) a summary artifact (`acceptance-summary.txt`) on every terminal path — ADR-055 §9 mandates
    this unconditionally; the plugin currently skips the write on precondition_unmet and malformed
    exits, leaving `acceptance_detail` legitimately `required: false`; after this change both paths
    write a summary, so `required: false` becomes `required: true`;
(4) name-matched input resolution via `ZBUILD_STAGE_INPUTS` (ADR-055 §1) instead of the hardcoded
    `local design_md="$artifact_dir/design.md"` at plugin.sh:239.

Additionally, the `|| true` on the main summary write (plugin.sh:538) must be removed — under
`set -euo pipefail`, a bare atomic_write failure is a genuine error, not a silent skip.

Backwards compatibility: `failures` and `fault` stay at the top level of the result JSON (not
moved under `data:`), because `plugins/agent/build/lib/context.sh` and `core/pipeline/verdict.sh`
read them there and neither is in this migration's scope.

**Decision.** Two focused manifest edits, ~40 lines of plugin.sh edits across four change sites,
and five test-helper updates to set ZBUILD_STAGE_INPUTS alongside the existing `cp design.md`.
A new integration test (`acceptance-gate-v2-contract-test.sh`) is the TDD red step and bears all
four [change] SPEC assertions.

---


### Scope rationale

**`tests/unit/summary-producers-test.sh`** — line 53–54 pins the `required` field for
`acceptance_detail`:
This asserts the OLD value `false`. Changing `acceptance_detail` to `required: true` in the
manifest makes this assertion fail — not because any constant was grepped away, but because the
4th pipe-delimited field (the `required` column) changes from `false` to `true`. The test label
("declared optional") and expected value both become wrong. In scope: the file must be updated
to assert `true` and reflect the new rationale.

**`docs/wiki/plugins/spec-acceptance.md`** — embeds a verbatim manifest YAML block that shows
the old shape (no `result_contract:`, no `acceptance_detail`, stale `source: stage:design` input
form). lint-doc-freshness validates prose structure, not manifest content, so CI will not fail;
nevertheless this is a contract-describing doc and must be updated for accuracy.

**`docs/adr/ADR-036-acceptance-contract-teeth.md`** — references the summary write path and
the acceptance_detail declaration; no edit required but cited by this PR.

**`docs/adr/ADR-054-stage-contract.md`** — defines the v2 result file contract; authoritative
reference; no edit.

**`docs/adr/ADR-055-inter-stage-data-contract-v2.md`** — §9 uses `acceptance-summary.txt` as
the worked example of an undeclared output; names this migration's exact changes as the
obligation. No edit; governs the migration.

**Files NOT in scope:** `plugins/agent/build/lib/context.sh` and `core/pipeline/verdict.sh`
read `failures`/`fault` at the top level — those fields are unchanged. `config/templates/simple.yaml`
references the `acceptance_gate` role (not the plugin id) — unchanged. `tests/integration/
self-host-contract-lib-redirect-test.sh` sources `plugin.sh` but does not call `acceptance_gate_run`
or inspect result JSON. `tests/unit/acceptance-disposition-classify-test.sh` and
`acceptance-negctl-test.sh` source internal helpers only — no result-shape assertions.

---


LOOP_COMPLETE
Where a design decision above conflicts with the plan, follow the design decision.

## ACCEPTANCE TESTS (you MUST make these pass)
You do NOT author or modify acceptance assertions. They were written from the design contract by a separate stage, before your implementation existed, and they are the contract you implement against. A failing assertion means YOUR CODE is wrong — fix the code. You MUST NOT weaken, delete, retag or re-author any assertion, and you must not edit the testfiles listed below; the spawn denies it and a mechanical guard checks it. If you believe an assertion genuinely does not test its SPEC, say so in your output and leave it alone: correcting it is the author stage's job, not yours (#2022, ADR-036).
Each test MUST contain an assert call whose label includes the [SPEC-n] tag for the SPEC it verifies (e.g. assert_eq "[SPEC-1] ..." exp act). The acceptance-gate (ADR-036) requires every SPEC-n to have a [SPEC-n]-tagged assertion. A CHANGE-behavior SPEC-n MUST have a tagged assertion that FAILS at the merge-base baseline and passes here (a tautological change-SPEC that passes without your implementation is rejected); a GUARD/invariant SPEC-n is tagged but NOT contorted to fail at baseline. See the per-id list below.
- SPEC-1: tests/integration/acceptance-gate-v2-contract-test.sh
- SPEC-2: tests/integration/acceptance-gate-v2-contract-test.sh
- SPEC-3: tests/integration/acceptance-gate-v2-contract-test.sh
- SPEC-4: tests/integration/acceptance-gate-test.sh tests/integration/acceptance-gate-reachability-test.sh tests/integration/acceptance-gate-quiet-test.sh tests/integration/acceptance-gate-inert-wiring-iter1-test.sh tests/integration/acceptance-guard-regressed-routes-design-test.sh

What each SPEC requires:
- [SPEC-1] result_contract:2 is present on every terminal exit path of acceptance_gate_run — precondition_unmet (both preconditions), malformed_acceptance_block, pass, and fail
- [SPEC-2] acceptance-summary.txt is written on the precondition_unmet path and on the malformed_acceptance_block path, so acceptance_detail exists on every terminal verdict (making required:true correct)
- [SPEC-3] design_md is resolved from ZBUILD_STAGE_INPUTS JSON index (jq -r .inputs.design) rather than the hardcoded $artifact_dir/design.md path — plugin.sh has zero grep hits for the literal string '$artifact_dir/design.md' after this change
- [SPEC-4] all existing acceptance-gate behavioral contracts are unchanged — verdict semantics, disposition classification, event emission, negctl/reachability levels, fault routing

### SPEC IDS YOU MUST COVER (acceptance gate, ADR-036)
Every SPEC id below needs at least one assertion whose label carries its [SPEC-n] tag; self-verify the FULL set is tagged before emitting LOOP_COMPLETE. A CHANGE-behavior SPEC (new behavior this change introduces) MUST have a [SPEC-n] assertion that FAILS at the merge-base baseline. A GUARD/invariant SPEC (behavior that must stay unchanged) is tagged but MUST NOT be contorted to fail at baseline.
- [SPEC-1] result_contract:2 is present on every terminal exit path of acceptance_gate_run — precondition_unmet (both preconditions), malformed_acceptance_block, pass, and fail
  → needs a `[SPEC-1]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-2] acceptance-summary.txt is written on the precondition_unmet path and on the malformed_acceptance_block path, so acceptance_detail exists on every terminal verdict (making required:true correct)
  → needs a `[SPEC-2]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-3] design_md is resolved from ZBUILD_STAGE_INPUTS JSON index (jq -r .inputs.design) rather than the hardcoded $artifact_dir/design.md path — plugin.sh has zero grep hits for the literal string '$artifact_dir/design.md' after this change
  → needs a `[SPEC-3]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
- [SPEC-4] all existing acceptance-gate behavioral contracts are unchanged — verdict semantics, disposition classification, event emission, negctl/reachability levels, fault routing
  → needs a `[SPEC-4]`-tagged assertion that tests exactly that (change → fails at baseline; guard → tagged, not contorted)
