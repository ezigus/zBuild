Scope is complete. The two integration tests (`tests/integration/artifact-persist-worktree-test.sh`, `tests/integration/prior-work-reuse-integration-test.sh`) and `plugins/tool/hydrate/plugin.sh` reference `_artifact_persist_restore` and `_artifact_persist_snapshot`, but neither the function signatures nor their observable behavior changes — the design adds tests for an existing dark code path with no library edits. The `data.snapshot` field assertions in SPEC-2/SPEC-3 target `persist-stage-test.sh` only; no other file pins that field. The `docs/wiki/plugins/persist.md` documents the existing schema and is not invalidated. No scope gaps found.
