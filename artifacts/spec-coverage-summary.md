## spec-coverage — covered

- All 8 explicit acceptance checkboxes map to at least one SPEC — exit-path coverage (SPEC-1/2/3/4), valid_verdicts (SPEC-7), no path literals (SPEC-14), router budgets with override precedence (SPEC-11/12), passing-run backward compat (SPEC-1/2/9), and primary output (SPEC-13); the remaining two checkboxes are universal DoD covered project-wide.

- every requirement the issue states maps to a declared SPEC
