## spec-correspondence — partial

- judged 19 SPEC(s): 12 correspond, 7 partial, 0 mismatch, 0 uncheckable, 0 unjudged

- SPEC-10 partial: The assertion confirms "verdict" appears somewhere in the `_review_lens_write_result` body and that the coercion grep does not false-positive on that body, but `grep -q 'verdict'` is satisfied by a bare variable reference, comment, or any non-jq occurrence — the "only as a jq field" constraint is not established.
- SPEC-11 partial: The existence check fully establishes the first half of the requirement, but the regex only rules out literals matching `lens-…` or `scope-manifest.md`; other hardcoded artifact path literals (e.g., `"review-output.json"`, `/tmp/artifacts/…`) would pass unchallenged, so the assertion does not establish that the body contains *no* hardcoded artifact path literals.
- SPEC-12 partial: The grep confirms `primary: true` appears somewhere in `manifest.yaml` but does not establish that it is specifically the `outputs[].lens_result` entry that carries the flag — any other key bearing `primary: true` would satisfy the assertion while leaving the requirement unmet.
- SPEC-14 partial: The assertion checks that '99' and "TURN BUDGET" each appear somewhere in the prompt via independent greps, but does not establish that 99 appears *within* the TURN BUDGET block — a prompt where 99 occurs in an unrelated section and TURN BUDGET appears elsewhere would satisfy both greps while leaving the "budget block reflects template value" constraint unmet.
- SPEC-16 partial: The assertion confirms all three named events are present and that exactly three `review_lens.`-prefixed lines appear in the provides section, but does not establish that no additional events under a different prefix exist there — an extra event with a non-`review_lens.` prefix would pass every check while violating the "exactly three events" requirement.
- SPEC-18 partial: The assertion confirms the three named forbidden fields are absent and that `id` and `required` are present, but does not establish the "only" constraint — any other field (e.g., `description`, `default`, `version`) could appear in an inputs entry and all checks would still pass, leaving the requirement unmet.
- SPEC-19 partial: The second grep matches the ADR-054 §7 citation anywhere in `manifest.yaml` — including inside a YAML field value — but the requirement specifies an "explanatory comment," which in YAML must appear on a `#`-prefixed line; the assertion does not enforce that constraint.

