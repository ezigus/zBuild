## spec-correspondence — partial

- judged 19 SPEC(s): 12 correspond, 7 partial, 0 mismatch, 0 uncheckable, 0 unjudged

- SPEC-9 partial: The assertion verifies all four v1 fields are present and checks score by exact value, but checks findings[] only by entry count (length == 1), not by element content — so it does not fully establish that the findings array is "unmodified" as the requirement states.
- SPEC-10 partial: The assertion establishes that coercion tokens are absent from both files and that the coercion grep does not false-positive on the _review_lens_write_result body, but it checks only for the plain string "verdict" in that body — it does not verify that verdict appears specifically as a jq field rather than in a comment, echo, or other non-jq form, so the "only as a jq field" clause of the requirement is unestablished.
- SPEC-11 partial: The assertion checks only for a specific subset of hardcoded artifact path literals — strings starting with `lens-` (in quoted form without variable expansion) and the literal `"scope-manifest.md"` — leaving other possible artifact path literals (e.g., `review-report.md`, `gate-result.json`, or any artifact name that does not match those patterns) outside its coverage.
- SPEC-12 partial: The assertion checks whether `primary: true` appears anywhere in `manifest.yaml`, but the requirement specifies it must be declared on the `outputs[].lens_result` entry specifically — the grep would pass if `primary: true` appeared on any other field or output, without establishing that `lens_result` bears it.
- SPEC-14 partial: The two `grep -q` checks are independent — confirming "99" appears somewhere in the prompt and "TURN BUDGET" appears somewhere in the prompt does not establish that 99 is specifically within the budget block; the sentinel value could satisfy the grep from unrelated prompt content while the budget block still reflects the manifest value.
- SPEC-16 partial: The assertion confirms all three named events are present and that the `provides:` block contains exactly three `review_lens.` references, but it extracts the entire `provides:` block via awk rather than specifically the `provides.events` sub-key — a passing run does not establish that the events are nested under `events:` as opposed to another sub-key within `provides:`.
- SPEC-18 partial: The assertion confirms the three named forbidden fields are absent and that `id` and `required` are present, but does not establish "only" — any other field besides `producer_stage`, `path`, and `type` (e.g. `description`, `default`) would pass unchallenged, leaving the exclusivity half of the requirement unverified.

