## spec-coverage — covered

- All 19 SPEC entries in the design's acceptance block map directly to the issue's explicit checklist items and the two "also land here" requirements (schema-gate parser recovery, ADR-063 budget guidance, rc=10 exhausted disposition), with no issue requirement left without a corresponding SPEC.

- every requirement the issue states maps to a declared SPEC
